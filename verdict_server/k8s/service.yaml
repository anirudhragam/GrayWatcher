apiVersion: v1
kind: Service
metadata:
  name: verdict-server
  namespace: graywatcher
  labels:                    
    app: verdict-server  
spec:
  type: ClusterIP
  selector:
    app: verdict-server
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http